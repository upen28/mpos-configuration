apiVersion: v1
kind: Service
metadata:
  name: mpos-svc
  labels:
    app: mpos-svc
spec:
  type: LoadBalancer
  selector:
    app: mpos
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8090
